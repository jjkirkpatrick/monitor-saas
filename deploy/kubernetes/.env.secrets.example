# Database Credentials
POSTGRES_USER=monitoring
POSTGRES_PASSWORD=changeme
POSTGRES_DSN=postgresql://monitoring:changeme@postgres:5432/monitoring?sslmode=disable

TIMESCALE_USER=monitoring
TIMESCALE_PASSWORD=changeme
TIMESCALE_DSN=postgresql://monitoring:changeme@timescaledb:5432/monitoring_metrics?sslmode=disable

# Redis Credentials
REDIS_PASSWORD=changeme

# Auth0 Configuration
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_AUDIENCE=your-auth0-audience

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=notifications@example.com
SMTP_PASSWORD=changeme

# External Service Integration
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# Grafana Admin Credentials
GRAFANA_USER=admin
GRAFANA_PASSWORD=changeme

# Note: Replace all 'changeme' values with secure passwords in production
# Generate secure passwords using: openssl rand -base64 32
